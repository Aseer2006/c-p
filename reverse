#include<stdio.h>
void main() 
{
    int n;
    int num;
    int rev=0;
    printf("enter no. ");
    scanf("%d" , &n);
    while ( n>0)
    {
    num=n%10;
    printf(" %d" , num);
    n=n/10;
    }
    
}
